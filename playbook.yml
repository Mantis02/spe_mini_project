- name: Pull image for calculator
  hosts: all
  tasks:
    - name: pull docker image
      shell: docker pull mantis02/spe-mini-project:latest

    - name: Run the container
      shell: docker run -itd mantis02/spe-mini-project:latest